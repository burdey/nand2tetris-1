CHIP Equal {
    IN a, b;
    OUT out;

    PARTS:
    And(a=a, b=b, out=AAndB);
    Not(in=a, out=NotA);
    Not(in=b, out=NotB);
    And(a=NotA, b=NotB, out=NotAAndNotB);
    Or(a=AAndB, b=NotAAndNotB, out=out);
}
